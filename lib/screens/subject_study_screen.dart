import 'package:flutter/material.dart';

class SubjectStudyScreen extends StatelessWidget {
  const SubjectStudyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Column(),
      ),
    );
  }
}
